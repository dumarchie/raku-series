#!/usr/bin/env raku
use lib 'lib';
use Series;

sub MAIN(Int :$elems = 2**20) {
    my Series $next;
    my \start = now;
    for ^$elems -> $value {
        $next .= new(:$value, :$next);
    }
    say sprintf "Construct series with $elems nodes: %0.3fs", now - start;
}
